
#include <iostream>
using namespace std;
int main() {
    int angka[5] = {1, 2, 3, 4, 5};
    for (int i = 0; i < 5 / 2; i++) {
        swap(angka[i], angka[4 - i]);
    }
    for (int i = 0; i < 5; i++) {
        cout << angka[i] << " ";
    }
    return 0;
}
